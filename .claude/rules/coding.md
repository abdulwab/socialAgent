# Coding Rules

- Follow existing style and keep work scoped to the active checklist gate.
- Use typed contracts and deterministic policy around LLM output.
- Comment only non-obvious logic; Roman Urdu is acceptable where helpful.
- Do not refactor unrelated code or delete legacy behavior before tested replacement.
- Run targeted tests, regression checks, leakage scans, and `git diff --check`.
